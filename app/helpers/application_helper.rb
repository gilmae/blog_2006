# Methods added to this helper will be available to all templates in the application.
module ApplicationHelper
#   def tags
#     @context[:tags] = Node.tags_count({:limit=>100, :order=>"name"}) if !@context[:tags]
#   end

end
